# Server Configuration
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Database Configuration - PostgreSQL (Aurora PostgreSQL)
# For Vercel + Aurora PostgreSQL, you'll get connection details after installation
DB_TYPE=postgresql
DB_HOST=your-aurora-postgres-host.postgres-aws.com
DB_PORT=5432
DB_NAME=foodism_db
DB_USER=your_username
DB_PASSWORD=your_password
DB_SSL=true

# Alternative: MongoDB (if you switch)
# DB_TYPE=mongodb
# MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/foodism_db

# Notes:
# - After installing Aurora PostgreSQL on Vercel, you'll get connection details
# - Replace the DB_* values above with your actual Aurora PostgreSQL credentials
# - For local development, you can use a local PostgreSQL instance
# - DB_SSL=true is required for Aurora PostgreSQL on AWS
